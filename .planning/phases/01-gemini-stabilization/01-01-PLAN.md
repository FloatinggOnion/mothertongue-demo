---
phase: 01-gemini-stabilization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/services/gemini.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - Test framework is installed and configured
    - Test command `yarn test` runs without errors
    - Gemini service tests can be executed and read test outputs
  artifacts:
    - path: vitest.config.ts
      provides: Test environment configuration
      min_lines: 5
    - path: src/services/gemini.test.ts
      provides: Unit test stubs for INFRA-01 and INFRA-02
      min_lines: 50
    - path: package.json
      contains: "vitest" in devDependencies
  key_links:
    - from: package.json
      to: vitest.config.ts
      via: test runner setup
      pattern: "vitest.*devDependencies"
    - from: src/services/gemini.test.ts
      to: src/services/gemini.ts
      via: import statement
      pattern: "from '@/services/gemini'"
---

<objective>
Establish test infrastructure required for Phase 1 (Gemini Stabilization) to validate retry logic and error handling.

Purpose: Wave 0 must complete before per-task testing becomes runnable. Vitest configuration and test scaffolding unblock all subsequent tasks that implement INFRA-01 and INFRA-02.

Output: Vitest installed and configured, test files created with stubs for evaluateConversation and callGemini behavior tests.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-gemini-stabilization/01-RESEARCH.md
@.planning/phases/01-gemini-stabilization/01-VALIDATION.md

Codebase sources:
@package.json
@src/services/gemini.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0a: Install test framework and validation library</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (current dependencies and version info)
  </read_first>
  <action>
Install vitest, coverage reporter, and make zod explicit:

```bash
yarn add -D vitest @vitest/coverage-v8
yarn add zod
```

Zod is already present as a transitive dependency in node_modules (version 4.3.5). This adds it to package.json as an explicit dependency to signal intentional use for request validation (INFRA-04).

Verify both commands complete successfully. After completion, confirm:
- package.json devDependencies contain "vitest" and "@vitest/coverage-v8"
- package.json dependencies contain "zod"
- yarn.lock is updated with resolved versions
  </action>
  <verify>
    <automated>
      grep -q '"vitest"' package.json && grep -q '"zod"' package.json && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - package.json updated with vitest and @vitest/coverage-v8 in devDependencies
    - package.json updated with zod in dependencies
    - yarn.lock refreshed without errors
  </done>
</task>

<task type="auto">
  <name>Task 0b: Create Vitest configuration file</name>
  <files>vitest.config.ts</files>
  <read_first>
    - No prerequisite files (new file creation)
  </read_first>
  <action>
Create `vitest.config.ts` at project root:

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    globals: true,
  },
});
```

Configuration details:
- `environment: 'node'` — for service and API route testing (no DOM/jsdom)
- `include: ['src/**/*.test.ts']` — discovers all test files in src directory
- `globals: true` — enables describe/it/expect without explicit imports

File location: `/Users/paul/Documents/programming/mothertongue-demo/vitest.config.ts`
  </action>
  <verify>
    <automated>
      test -f vitest.config.ts && grep -q "environment: 'node'" vitest.config.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - vitest.config.ts exists at project root
    - Contains `environment: 'node'`
    - Contains `include: ['src/**/*.test.ts']`
    - Contains `globals: true`
  </done>
</task>

<task type="auto">
  <name>Task 0c: Create gemini service test file with behavior stubs</name>
  <files>src/services/gemini.test.ts</files>
  <read_first>
    - src/services/gemini.ts (to understand exported functions and error handling patterns)
  </read_first>
  <action>
Create `src/services/gemini.test.ts` with test scaffold. This file contains placeholder tests that will be implemented in Plan 02. Structure it to match INFRA-01 and INFRA-02 requirements:

```typescript
import { describe, it, expect, vi } from 'vitest';

describe('Gemini Service', () => {
  describe('callGemini() — exponential backoff retry logic', () => {
    it('INFRA-02: should retry exactly 3 times on 429 error with exponential backoff', () => {
      // Placeholder — implemented in Plan 02
      // Mock GoogleGenAI to throw 429 on attempts 1-2, succeed on attempt 3
      // Assert: 3 total attempts made
      // Assert: Delays fall within ranges: 2-4s, 8-16s (not used if success on 3rd)
      expect(true).toBe(true);
    });

    it('INFRA-02: should NOT retry on 400 error', () => {
      // Placeholder — implemented in Plan 02
      // Mock GoogleGenAI to throw 400
      // Assert: Thrown immediately without retry
      expect(true).toBe(true);
    });

    it('INFRA-02: should NOT retry on 403 error', () => {
      // Placeholder — implemented in Plan 02
      // Mock GoogleGenAI to throw 403
      // Assert: Thrown immediately without retry
      expect(true).toBe(true);
    });

    it('INFRA-02: should retry on 503 and 502 errors', () => {
      // Placeholder — implemented in Plan 02
      // Mock to throw 503 then 502, then succeed
      // Assert: Both errors trigger retry logic (not immediate throw)
      expect(true).toBe(true);
    });
  });

  describe('evaluateConversation() — error propagation', () => {
    it('INFRA-01: should throw on JSON parse failure instead of returning fallback', () => {
      // Placeholder — implemented in Plan 02
      // Mock callGemini to return invalid JSON
      // Assert: evaluateConversation() throws (does not catch and return default all-5s scores)
      expect(true).toBe(true);
    });

    it('INFRA-01: should throw on Gemini API error', () => {
      // Placeholder — implemented in Plan 02
      // Mock callGemini to throw error
      // Assert: evaluateConversation() propagates error to caller
      expect(true).toBe(true);
    });
  });
});
```

Key points:
- All tests use placeholder `expect(true).toBe(true)` — will be filled in Plan 02
- Comments document expected behavior for future implementation
- Test names match requirement IDs (INFRA-01, INFRA-02) for traceability
  </action>
  <verify>
    <automated>
      test -f src/services/gemini.test.ts && grep -q "INFRA-01" src/services/gemini.test.ts && grep -q "INFRA-02" src/services/gemini.test.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - src/services/gemini.test.ts exists
    - Contains 6 test cases (stubs)
    - All tests reference INFRA-01 or INFRA-02
    - All tests pass with placeholder expectations
  </done>
</task>

<task type="auto">
  <name>Task 0d: Verify test infrastructure works end-to-end</name>
  <files>package.json, vitest.config.ts, src/services/gemini.test.ts</files>
  <read_first>
    - vitest.config.ts (test command configuration)
    - src/services/gemini.test.ts (test discovery and count)
  </read_first>
  <action>
Run the test suite to confirm vitest is configured and running correctly:

```bash
yarn test
```

Expected behavior:
- Vitest discovers src/services/gemini.test.ts
- All 6 placeholder tests execute
- All 6 tests pass (because expectations are expect(true).toBe(true))
- Output includes summary like "6 passed"
- No errors or warnings

If yarn test fails:
1. Check vitest is in devDependencies: `grep vitest package.json | grep devDependencies` should show vitest
2. Verify vitest.config.ts syntax: `node -e "require('./vitest.config.ts')"`
3. Check gemini.test.ts for syntax errors: `tsc --noEmit src/services/gemini.test.ts`

The test suite should complete in under 2 seconds.
  </action>
  <verify>
    <automated>
      yarn test 2>&1 | grep -q "passed" && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <done>
    - `yarn test` completes without errors or failures
    - Test summary shows "6 passed" (all placeholder tests pass)
    - Vitest is ready for Plan 02 implementation
    - No existing functionality changed (test infrastructure only)
  </done>
</task>

</tasks>

<verification>
After all Wave 0 tasks:

1. Confirm package.json has vitest, @vitest/coverage-v8, and zod
2. Confirm vitest.config.ts exists with correct Node environment
3. Confirm src/services/gemini.test.ts exists with 6 test stubs
4. Confirm `yarn test` runs and reports "6 passed"
5. Confirm no existing app tests broke (this is a new file)
</verification>

<success_criteria>
- [ ] vitest and zod installed
- [ ] vitest.config.ts created and valid
- [ ] 6 test stubs in gemini.test.ts
- [ ] `yarn test` passes with 6 passing tests
- [ ] Test infrastructure is ready for Plans 02–05
</success_criteria>

<output>
After completion, create `.planning/phases/01-gemini-stabilization/01-01-SUMMARY.md`
</output>
